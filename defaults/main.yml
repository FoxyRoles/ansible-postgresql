---

db_collation: utf8_general_ci

postgresql_local_passdb: "{{ local_passdb | default('~/ansible/passwords') }}"
postgresql_pass_len: 24
postgresql_pass_complexity: "ascii_letters,digits" # ascii_letters,digits,hexdigits,punctuation
